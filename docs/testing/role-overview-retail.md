# 正式服角色总览实机清单

状态：待实机验证。测试包中的自动化通过不代表客户端已支持。

- 确认角色总览入口可见，登录和 `/reload` 后不会自动打开窗口。
- 确认当前 BGLite P2 实例默认显示，P1 实例默认隐藏。
- 在设置页恢复旧赛季实例，确认 2912、2939、2913、1592 均为独立表头且不会合并。
- 确认 API 不可用或受保护时对应单元格/栏目安全留空，不显示错误的零、不报 Lua 错误。
- 确认默认摘要只有主专业、武器、饰品、金币；装备详情可从设置恢复。
- 确认没有泰坦升级物品、泰坦货币或泰坦特殊装备栏目。
- 验证隐藏/恢复栏目、Shift 跨服、中键或 Ctrl+左键固定、拖动、刷新和关闭。
- 记录客户端版本、插件版本、分辨率、UI 缩放、错误日志和脱敏截图。
