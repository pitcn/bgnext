# BGNext 0.8.8

本版本修复安装包在部分发布与安装工具中的目录兼容问题，插件功能与 0.8.7 相同。

- 发布包改用标准 ZIP 目录格式，避免网易 DD 等工具把插件目录识别为异常层级。
- 新增发布检查，后续构建会拒绝非标准的 ZIP 路径分隔符。
- 不改变拍卖、交易、结算、心愿、角色总览或任何保存数据。

## 安装

完全退出游戏并备份 WTF，然后完整替换 AddOns 中的 BGNext 文件夹；不要同时启用 BGLite、BiaoGe 或依赖它们的扩展。
