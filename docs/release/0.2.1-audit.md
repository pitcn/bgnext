# BGNext 0.2.1 发布审计

## 范围

- 移除主界面和设置页对已停用 `ReceiveMainFrame` 的四处访问。
- 将新增的简体中文临时对账文案由布尔标记改为字符串。
- 增加两类启动回归测试。

## 发布门禁

- Lua 测试、基线完整性、差异格式与发布包检查均须通过。
- 发布包根目录须为 `BGNext/`，且不得包含隔离的旧接收和历史模块。
- GitHub CI 与发布资产校验结果记录在对应发布页。
