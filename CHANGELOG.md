# 变更日志

本文件记录 BGNext 的重要变化。版本条目按时间倒序排列。

## 未发布

## 0.7.0 - 2026-09-04

### 使用方式

- 新增完整、基础和自定义三种增强模式；基础模式仅保留核心账单与安全功能，自定义模式可按功能组开关增强功能，切换模式不会删除已保存数据。
- 新增游戏内说明书，集中列出主要功能、快捷键、命令、权限要求、危险操作提示和隐私边界；更新日志可向下查看 0.1.0 以来的历代版本摘要。

### 拍卖

- 重做待拍队列窗口的视觉层级、尺寸与按钮布局；关闭窗口不会清空队列，清空操作单独确认。输入框支持聚焦后 Shift+点击背包装备，也支持粘贴物品链接或直接输入物品 ID。
- Boss 拾取窗口在玩家具有团长或拾取职责时显示拍卖入口；入口复用权限、战斗、活动拍卖、价格与团队范围等发送前检查。
- 修复预设 Alt+右键仍打开普通起拍窗口的问题；原生表格 Frame 也能进入预设直拍流程。
- 修复混合 BGNext/BGLite 团队中同物品拍卖倒计时不同步、卡片误隐藏，以及结束时买家与成交金额缺失的问题；有效出价按实时团队名单校验。

### 交易与结算

- 修复特定事件顺序、切团及相同物品场景下成功交易或邮件记录遗漏；未知金币继续保持未知，不伪造成 0。
- 当前团账单支持由团长或拾取职责玩家标记“退货待处理”，结算前检查会提醒人工核对；不会自动退款，也不会自动改写原买家或金额。

### 角色总览

- 熊猫人角色总览新增四个默认关闭的可选项目：天神首胜、农场收菜、四天神和斡耳朵斯。证据不足时显示未知，不猜测完成状态。
- 农场收菜只在当前角色于日歌农场实际拾取已知作物后记录当天完成；其他项目依赖当前客户端可用的任务或随机副本状态，仍需实机验证。

### 上游与兼容

- 上游基础升级到官方 BGLite 纯净版 2.4.2，并继续保留 BGNext 的本地隐私、发送前安全检查和基线完整性校验。
- 插件登记覆盖经典永久 60、周年服 TBC、周年时光服、熊猫人和正式服；各版本新增功能的实机状态以兼容性矩阵为准。巫妖王与大地的裂变仍在适配，探索赛季不提供角色总览。

## 0.6.0 - 2026-09-03

本次更新集中修复真实团队中的拍卖结果、交易记录与通报，并加入团长待拍队列和自动清空保护。

### 自动清空保护

- 「进本自动清空表格」开启时，进入新副本进度且目标表格仍有未结算内容，先弹出明确确认，说明将同时清除表格内容与当前团的结算记录；空表格不打扰。
- 确认后才重新校验目标表格仍有内容并沿用现有清空路径清空一次；拒绝、Esc 或取消不会清空表格，也不会清掉匹配的当前团结算记录。
- 手动清空仍为明确删除，不提供恢复；当前团结算仍保持单团、最长七日及现有过期/新团替换边界。不新增持久字段、通信、上传或遥测。

### 团长待拍队列

- 团长或主拾取者可把装备加入、移出待拍队列并调整顺序；队列行显示物品提示、数量、预期起拍价及其价格来源（单件价／基础价／手动输入），无法安全解析价格时明确要求手动输入。
- 价格从现有团长起拍价方案只读预填，不修改原方案；只有点击当前行明确的确认操作后才会走原拍卖发送路径，切换到下一件不会自动发送。
- 无权限、战斗限制、物品无效、价格冲突或已有拍卖进行中时阻止发起并显示原因。
- 队列仅属于当前运行会话和当前团／表格，离团、切换团本、清空表格或重载后丢弃，首版不持久化；不引入新的拍卖协议、自动聊天消息或后台高频轮询。
- 待拍窗口补充明确关闭按钮并统一按钮外观；底栏在全部入口创建后重新排布，角色总览固定在最右侧，不再与待拍或结算按钮重叠。

### 拍卖卡片结果展示

- 侧边装备拍卖卡片倒计时结束后，无论之前折叠还是展开，都会展开并清晰显示最终买家和成交金额；流拍或取消显示对应状态，不虚构买家或金额。
- 结果只停留约 15 秒（有界、可读），之后按原有淡出并释放卡片，不永久保留、不新建跨团历史。
- 结果展示期间竞价与自动出价保持停止：折叠按钮禁用、取消与输入框隐藏、「自动出价」按钮隐藏、自动出价计时器在结束态取消；不重新启用竞价或自动出价，也不改变自动出价原有停止条件与权限检查。
- 倒计时结束只执行一次（结束态幂等）；成交、流拍、取消三条路径都会立即停止倒计时。折叠卡片结束强制展开后会做一次有界重排，避免同时拍卖的其他卡片重叠，不引入周期性布局。
- 多件同时拍卖、重复或延迟回调各自独立清理。不改协议、记账、交易记录或持久化范围，不新增持久字段或通信。

### 交易记录与结算

- 交易确认时从当前交易窗口采集双方物品、数量与金币；同物品多件不会再因去重而漏记，无法证明的数量或金币保持未知，不伪造为 0。
- 当前团交易记录同时展示实际交付事实和核对状态；一笔交易可以显示“已交易 · 待核对”，避免把装备已交付误写成账目已结清。
- 重新接通“交易通报”设置：交易明确成功、取消或背包已满时按玩家选择发送一次，单纯关闭窗口不通报；通报不保存聊天内容或跨团历史。

### 价格预设与列表

- 表格装备 Alt+右键按预设开拍时，发送前统一复核权限、战斗状态、当前拍卖、方案、价格、客户端与团队范围；条件变化会安全停止并说明原因。
- 修复表格装备的组合键被普通右键删除抢先处理：Ctrl+右键只编辑单件起拍价，Alt+右键只进入预设开拍；单人或无权限时会安全拦截，绝不删除装备。
- 价格列表按实际内容区和本地化文案自动重排，窗口缩放时合并连续刷新；最后一行保持可滚动，不在动画期间反复扫描全表。

### 角色总览

- 正式服团本 CD 改为按单个 Boss 及普通、英雄、史诗难度分别统计；部分击杀显示“已击杀数/总 Boss 数”，仅全部击杀才显示完成。API 证据不足时留空，不伪造完成状态。

## 0.5.0 - 2026-09-03

- 心愿优先级三档均显示标识：备选为灰蓝、次 BIS 为淡紫、BIS 为金色；空槽位不显示，不改变装备品质颜色。

### 本地化

- 修复简中“存储与隐私”设置页将布尔值作为文字传入而报错的问题，并检查实际加载的各语言文案类型。
- 补齐价格预设与角色总览新功能的简中、繁中和英文文案；非简中、繁中客户端现在统一回落英文。
- 修复部分英文文案仍显示中文、明显错译及格式化占位符不一致的问题。
- 价格预设页的关键按钮会按翻译后的文字宽度调整，降低英文标签被截断的风险。
- 新增本地化自动检查，后续玩家可见功能必须同时提供简中、繁中和英文。

### 拍卖计时

- 同时拍卖两件相同装备时，任意一件收到有效出价后，所有相同装备的截止时间会一起刷新。

### 价格预设

- 输入基础价或单件价后直接点击别处也会保存，不再必须按回车，避免界面显示新价格但开拍仍使用旧价格。
- 团长在表格装备上按 Alt+右键时，先应用当前方案的单件价或基础价，再沿用原拍卖校验流程直接开拍；无法安全解析价格时保留拍卖窗口，不会直接发送。
- 在表格装备上按 Ctrl+右键可直接修改当前方案的单件起拍价，不再误触普通右键清除。
- 团长价格导入支持识别旧版 BGLite 单团本价格字符串，并在预览确认后转换成 BGNext 方案；BGNext 仍只导出自己的版本化格式。
### 性能

- 角色总览改为按事件只读取发生变化的数据：金币、装备、团本进度、货币、背包物品和专业不再互相触发完整采集。
- 短时间内连续发生的同类事件会合并处理；角色总览隐藏时仍保存最新角色数据，但不执行界面重绘。
- 背包“已有装备”标记的连续刷新请求会合并，减少背包变化时的重复全表扫描。

### 性能与隐私

- 设置中新增“存储与隐私”页，可检测旧版历史数据是否仍保存在本地，并由玩家手动清理以释放其占用的内存。
- 清理操作不会读取或迁移旧历史内容，必须经过不可恢复操作的二次确认；当前账单、设置、心愿清单、当前团结算和角色总览不受影响。
- 登录、升级、重载和取消操作都不会自动删除旧数据。

### 心愿清单

- 每件心愿装备新增三级本地优先级：备选 < 次 BIS < BIS。新选或替换装备默认备选，重新确认同一件装备保留原有优先级；内部继续使用稳定的 `backup`/`normal`/`core` 枚举。
- 心愿格上滚轮循环切换优先级，独立文字徽标在装备格下方显示三档：灰蓝备选、淡紫次 BIS、金色 BIS。保留装备图标、名称宽度和装等，长列表可滚动；悬停可查看完整解释。
- 导出文本仅对非默认优先级追加 `@priority` 标记；旧版本导出的心愿字符串可原样导入并全部按"次BIS"处理；缺失、未知或损坏的优先级标记安全回落为默认值，不会拒绝其余有效数据。导入失败仍保持原子性。
- 旧存档无需迁移：原有纯物品 ID 仍按“次 BIS”读取；新选装备以明确的备选优先级保存和导出。旧字符串含义不变，删除心愿时其优先级随之删除，不产生孤立数据。
- 本地心愿达成与拍卖提醒会附带命中的优先级，例如"心愿：核心提升"。优先级不进入任何聊天或插件消息，不发送、不同步给团队，也不记录其他玩家。
- 优先级滚轮、提示、重载保留及旧字符串导入已获玩家反馈通过；最新徽标布局和默认备选逻辑有自动化覆盖，未新增全客户端实测声明。

### 当前团结算

- 新增「结算前检查（当前团）」只读检查清单：汇总未核对交易、未处理欠款、缺少买家或金额的装备、销售交付缺少交易证据的账单行、分金人数（含小数与无效值）与净收入异常，以及失败或待核对的邮件，显示异常总数并按类型分组展示。
- 每条结果给出原因和真实定位入口（打开对应的交易/邮件记录，或切换到团本表格）；无法定位时在文字中说明位置，不做伪造跳转。全部检查通过且证据充分时显示「可以结算」；无当前团、空账单或数据缺失时显示「待确认」，不会误判为通过。
- 检查在打开页面和相关编辑事件时刷新，不再每秒扫描账单；过期检查使用关闭即取消的一次性计时器。关闭窗口后释放派生报告和条目引用。
- 当前团交易记录增加可选的交付方向；旧记录仍可读取，方向或数量证据不足时保持待核对。堆叠或打包交易不会被自动当作单件已结清。

## 0.4.0 - 2026-09-01

本次更新加入团长起拍价与个人心理价预设、团队拍卖就绪检查，以及可切换的现代预览外观。

### 价格预设

- 团长可按团本保存多套命名起拍价方案，包含基础价和单件覆盖价；可新建、复制、重命名、删除、切换和导入导出。
- 团员可为自己的角色保存单件心理价，按角色、客户端、服务器和团本隔离。
- 打开现有拍卖或出价窗口时自动预填已保存价格，团长和团员仍需手动确认开始或出价；多件装备价格不一致或无法安全解析时不预填。
- 价格预设只保存在当前玩家本地，不新增插件通信或游戏外传输。

### 团队拍卖就绪检查

- 进入团队后底部显示“团队拍卖：已就绪 X/Y”，不再显示容易误解的公会插件统计。
- 悬停可逐人查看状态：已就绪、插件已响应但拍卖端未响应、未响应、离线。
- “未响应”只代表尚未收到回复，不代表确定未安装。
- 团长或主拾取者点击可手动重新检测，请求有限流；检测结果不影响发起拍卖。

### 界面现代化（预览外观）

- 新增可切换的 BGNext 预览外观，保持原版账单布局、半透明背景、行列容量和一张图截图习惯。
- 经典外观仍为默认，可随时切换；预览主题对账单、价格预设页和部分高频页面统一品牌颜色、字号与控件状态。
- 价格预设页改为高密度双列展示，充分利用窗口空间。
- 移除装饰性 Boss 模型，减少视觉干扰；调整字号与颜色提升可读性。

本版本不新增插件通信协议或游戏外传输。价格预设、主题偏好和团队就绪状态只保存在本地；永久 60、周年 TBC、周年时光服、熊猫人和正式服的界面与功能仍需分别完成游戏内实机验证。

## 0.3.1 - 2026-09-01

### 修复

- 修复在主表格装备格按住 Alt 并点击右键时无法直接发起拍卖的问题。
- 修复团长或物品分配者自己拍得装备时，角色名是否带服务器后缀不一致会导致记账提示不出现的问题。
- 修复拍卖记录买家显示为白色的问题；新记录从当前团队名单获取职业信息，已有职业信息的记录也会重新使用职业颜色。
- 修复最近一次拍卖交易匹配与团长自交易状态使用不同姓名格式时可能判断错误的问题。

### 装备过滤

- 专精和自定义方案旁新增固定的红叉按钮，可一键临时关闭全部装备过滤并显示所有装备。
- 关闭过滤不会删除或修改原方案；再次点击红叉、选择任意方案、选择跟随专精或重置方案都会恢复过滤。

本版本不新增插件通信或游戏外传输。新增的过滤启用状态只保存在当前角色本地。上述修复已通过自动化测试，仍需在实际游戏客户端完成操作验证。

## 0.3.0 - 2026-09-01

### 装备过滤

- 内置方案改为按当前客户端版本和角色专精提供对应默认规则；切换专精时，处于“跟随专精”状态的方案会同步切换，玩家手动选择或自定义的方案不会被覆盖。
- 旧天赋树客户端的内置方案使用可辨识的方形天赋技能图标，不再把大尺寸天赋页背景裁进快捷按钮；早期测试包已保存的错误纹理会在登录后安全更新。
- 默认规则分别处理武器、护甲、主属性和版本实际存在的装备词缀。旧版本猎人仍可使用敏捷长柄、法杖和拳套等属性武器；现代生存猎人与远程猎人按各自专精处理。
- 修复非正式服无法可靠识别装备主属性的问题。优先使用暴雪物品属性接口，接口暂不可用时回退本地 Tooltip；未知、尚未加载或没有主属性的装备继续保守放行。
- 不同随机词缀的完整物品链接分别缓存，避免相同物品 ID 的不同变体互相影响。

### 界面与姓名

- 非正式服的本服角色姓名只显示角色名；正式服跨服角色继续保留服务器信息。已知职业统一使用职业颜色，未知职业保持默认颜色。
- 小地图按钮悬停约 0.2 秒后可以预览角色总览；快速划过不会闪窗，已经固定的总览不会因鼠标离开而关闭。
- 修复小地图右键入口菜单点击外部后无法关闭的问题。菜单控件在字体初始化完成后创建，避免部分客户端加载时出现 `SetFont` 报错。

### 修复

- 修复交易对象在“角色名”和“角色名-服务器”两种形式之间不一致时，装备已交付但拍卖记录仍显示“已拍未交易”的问题。同一物品一次交付多件时，现在会按实际数量更新，不会把其余记录误标为已交易。
- 修复姓名缩短后可能影响交易、欠款、退款、对账和拍卖记录身份匹配的问题；界面显示名与内部规范身份保持分离。

### 性能

- 移除团队中每两秒执行一次的欠款／罚款全表扫描。相关鼠标提示现在只在实际需要时构建缓存，并在买家、金额、欠款或当前团本变化后按需刷新；关闭功能或不在团队时不会扫描。
- 鼠标悬停装备时，高亮 Frame、闪烁动画和计数文字现在会回收复用，不再随悬停次数持续创建。
- 背包和当前可见表格使用可失效的物品索引；背包内容、表格文字、团本或可见视图变化后按需重建，状态不变时不再为每次悬停全量扫描。
- 关闭“高亮对应装备”后，原生背包悬停会在启动延迟和读取物品链接前直接返回。

本次优化不新增保存字段、游戏内通信或游戏外传输。对象池和索引已通过 1000 次循环模拟，仍需在原生背包及支持的第三方背包中完成游戏内冒烟验证。

本版本不新增插件通信协议或游戏外传输。永久60、周年TBC、周年时光服、熊猫人和正式服仍需分别完成对应功能的游戏内冒烟验证。

## 0.2.3 - 2026-08-30

这次更新主要修复正式服显示问题，并让表格在不同分辨率和窗口大小下更容易使用。

### 正式服

- 修复心愿清单同时显示普通、英雄和史诗难度时，史诗难度区域跑到窗口外的问题。
- 修复部分正式服环境无法记录当前登录角色，导致角色总览为空的问题。
- 对尚未录入掉落资料的正式服团本显示明确提示；仍可把装备拖入表格，或选中格子后按住 Shift 点击装备，手动加入个人心愿。

### 界面缩放

- 表格会根据当前屏幕宽高自动限制实际缩放，减少低分辨率、窗口模式和宽表格超出屏幕的情况。
- 保留玩家设置的首选缩放比例；切换到较窄的表格后可以恢复原来的比例。
- UI 缩放或分辨率发生变化时会自动重新适配；窗口边缘被裁切时会回到屏幕中央。

### 兼容操作

- 新增 `/biaoge` 兼容命令，可像 `/bgn` 一样打开 BGNext；`/biaoge role` 可打开自有角色总览。
- `/bgn` 和 `/bgnext` 仍是对外推荐的命令；同时启用其他表格插件时，命令归属仍可能受加载顺序影响。

本次没有新增保存字段、游戏内通信或游戏外传输。正式服掉落资料仍在逐步适配；缩放逻辑已通过自动测试，但不同客户端、分辨率和窗口模式仍欢迎继续反馈实机结果。

## 0.2.2 - 2026-08-30

这次更新修复了跨客户端拍卖兼容问题，并把拍卖发起窗口改得更直接。

### 修复

- 修复拍卖编号带小数时被安全校验误拦截，导致部分客户端无法开始拍卖或继续出价的问题。该修复位于共享协议层，对永久60、周年TBC、周年时光服、熊猫人和正式服统一生效。
- 修复小地图入口菜单打开后，再次右键无法关闭的问题。
- 货币接口返回占位值 `0` 时不再显示错误的“每周上限 0”；客户端提供真实正数上限时仍正常显示。

### 拍卖界面

- 移除需要玩家选择的“第一代/第二代拍卖”。
- 移除只有“常规模式”一个选项的拍卖模式菜单。
- 移除第二代专属重置阈值设置，窗口仅保留拍卖时长、数量、起拍价和开始按钮。
- 所有发起入口统一使用兼容旧插件的第一代常规协议；不会双发两代消息，也不会生成重复拍卖框。
- 保留自动出价保护、金额和权限校验等不改变通信格式的优化，并继续接收其他兼容插件发来的第二代拍卖。

不同客户端的游戏内容和暴雪 API 返回仍可能存在差异，未取得真实数值时 BGNext 会隐藏字段，不会用硬编码数据补齐。

## 0.2.1 - 2026-08-30

这是针对 0.2.0 的启动热修复。

### 修复

- 修复打开插件或设置页时仍访问已停用的旧接收窗口而产生的报错。
- 修复简体中文客户端初始化临时对账按钮时把本地化标记当作文案使用而产生的报错。

除上述修复外，功能、存档和隐私边界与 0.2.0 保持一致。

## 0.2.0 - 2026-08-29

这次更新统一了 BGNext 的插件身份，并加入几项常用操作和安全改进。

### 新增

- 使用全新的 BGNext 图标。
- 小地图按钮现在可以打开金团表格、角色总览和设置。
- 恢复角色总览快捷键。
- 聊天中明确显示的 YY 号码可以手动点击复制，包括普通密语和战网密语。

### 优化

- 插件安装目录由 `BGLite` 更名为 `BGNext`。
- 调整小地图按钮和角色总览入口的点击方式，让常用功能更容易找到。
- 加强拍卖金额校验和消息限流，减少异常消息对正常拍卖的影响。
- 对账数据仅用于当前游戏会话，不再保存其他玩家的历史账单。

### 升级提醒

- 从旧版本升级时，请先删除 `Interface\AddOns\BGLite` 文件夹，再安装新的 `BGNext` 文件夹。
- 不要删除 `WTF` 文件夹或 SavedVariables 中的 `BiaoGe.lua`；原有设置和本地数据会继续使用。
- BGNext 不会自动删除旧插件目录。

### 兼容与隐私

- 继续兼容 BGLite 用户参与当前团队拍卖。
- 保留原有本地存档名称，升级不会主动清空玩家数据。
- YY 复制只处理当前可见消息，不保存聊天历史，也不会自动发送内容。

## 0.1.0 - 2026-08-29

BGNext 的第一个社区版本以 BGLite 2.4.0 为兼容基础，重点补齐个人整理工具，并收紧旧功能的数据边界。

### 新功能

- 重新制作个人心愿清单，支持按副本、难度和首领选择装备，以及本地提醒和手动导入、导出。
- 增加装备过滤方案，方便按自己的职业和需求整理掉落列表。
- 增加自有角色总览，可查看本人角色的团本进度、专业、装备和版本适用资源；周年时光服已完成主要流程验证，其他已登记客户端仍以兼容性矩阵为准。
- 增加当前团购物、交易和邮件核对，只保留最近一个团，最长七天。
- 增加“关于”“更新日志”和“感谢名单”，说明项目来源、版权、隐私边界与公益承诺。

### 修复和优化

- 修复自动出价停止、重复顶价、多个拍卖框重叠，以及自己为最高出价者时提示不清楚的问题。
- 修复心愿清单切换副本或难度后列表不更新、同一物品出现在多个首领或难度时无法分别选择等问题。
- 修复交易价格回填、专精过滤初始状态、账单职业染色、角色总览布局与提示等细节。
- 修复熊猫人客户端勇气点数“本周获得”被放大 100 倍的问题。
- 角色总览采用按需刷新、事件合并和界面对象复用，避免后台持续轮询和重复创建控件。

### 安全与隐私

- 拍卖控制消息只接受当前团长或主拾取者；版本查询增加团队成员校验和内存限流。
- 停止加载旧整表接收、跨团交易历史和邮件历史功能，不读取或迁移这些旧数据。
- 发布包改为从实际运行清单生成，不再把未加载的历史源码一起分发。
- 新增功能数据保存在本地，不含遥测或游戏外自动同步；主动导入、导出会先展示内容。
- 对 BGLite 2.4.0 的运行文件改动逐项登记并由自动检查验证，不把上游代码声明为 BGNext 原创。

## 2.4.0-baseline - 2026-08-26

### 基线

- 导入活动渠道提供的 BGLite 2.4.0 上游分发快照。
- 保留 188 个上游文件，未修改任何运行文件。
- 建立唯一初始提交 `9e0b119c66a644cce0083b5ffe4e59c6c946d0f1`，作为后续完整性检查依据。
