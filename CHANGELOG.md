# 变更日志

本文件记录 BGNext 的重要变化。版本条目按时间倒序排列。

## 待发布

### 修复

- 修复交易对象在“角色名”和“角色名-服务器”两种形式之间不一致时，装备已交付但拍卖记录仍显示“已拍未交易”的问题。同一物品一次交付多件时，现在会按实际数量更新，不会把其余记录误标为已交易。
- 修复部分怀旧服客户端中，小地图右键入口菜单在鼠标移出后仍一直停留的问题。菜单控件改为在字体初始化完成后创建，避免加载时因字体路径尚未就绪而报错。修复仅管理 BGNext 自己的入口菜单，不改变其他下拉菜单或插件的点击行为。
- 修复非正式服装备过滤无法可靠识别主属性的问题。力量、敏捷和智力现在通过暴雪物品属性接口统一判断；接口暂不可用时才回退到本地 Tooltip，并对未知或无主属性装备保守放行。
- 装备过滤会按完整物品链接区分随机词缀或不同物品变体。旧存档中的内置职业方案若缺失主属性设置，会安全补齐职业默认值；玩家主动清空或修改过的选择不会被覆盖。
- 新建或重置纯物理职业方案时默认过滤法术强度，纯法系职业方案默认过滤攻击强度；同一职业包含物理与法系玩法的混合职业仍由玩家按专精手工选择，避免误过滤。

本次修复不新增保存字段、游戏内通信或游戏外传输。各客户端共享逻辑已通过自动测试，永久60、周年TBC、周年时光服、熊猫人和正式服仍需分别完成实机冒烟验证。

## 0.2.3 - 2026-08-30

这次更新主要修复正式服显示问题，并让表格在不同分辨率和窗口大小下更容易使用。

### 正式服

- 修复心愿清单同时显示普通、英雄和史诗难度时，史诗难度区域跑到窗口外的问题。
- 修复部分正式服环境无法记录当前登录角色，导致角色总览为空的问题。
- 对尚未录入掉落资料的正式服团本显示明确提示；仍可把装备拖入表格，或选中格子后按住 Shift 点击装备，手动加入个人心愿。

### 界面缩放

- 表格会根据当前屏幕宽高自动限制实际缩放，减少低分辨率、窗口模式和宽表格超出屏幕的情况。
- 保留玩家设置的首选缩放比例；切换到较窄的表格后可以恢复原来的比例。
- UI 缩放或分辨率发生变化时会自动重新适配；窗口边缘被裁切时会回到屏幕中央。

### 兼容操作

- 新增 `/biaoge` 兼容命令，可像 `/bgn` 一样打开 BGNext；`/biaoge role` 可打开自有角色总览。
- `/bgn` 和 `/bgnext` 仍是对外推荐的命令；同时启用其他表格插件时，命令归属仍可能受加载顺序影响。

本次没有新增保存字段、游戏内通信或游戏外传输。正式服掉落资料仍在逐步适配；缩放逻辑已通过自动测试，但不同客户端、分辨率和窗口模式仍欢迎继续反馈实机结果。

## 0.2.2 - 2026-08-30

这次更新修复了跨客户端拍卖兼容问题，并把拍卖发起窗口改得更直接。

### 修复

- 修复拍卖编号带小数时被安全校验误拦截，导致部分客户端无法开始拍卖或继续出价的问题。该修复位于共享协议层，对永久60、周年TBC、周年时光服、熊猫人和正式服统一生效。
- 修复小地图入口菜单打开后，再次右键无法关闭的问题。
- 货币接口返回占位值 `0` 时不再显示错误的“每周上限 0”；客户端提供真实正数上限时仍正常显示。

### 拍卖界面

- 移除需要玩家选择的“第一代/第二代拍卖”。
- 移除只有“常规模式”一个选项的拍卖模式菜单。
- 移除第二代专属重置阈值设置，窗口仅保留拍卖时长、数量、起拍价和开始按钮。
- 所有发起入口统一使用兼容旧插件的第一代常规协议；不会双发两代消息，也不会生成重复拍卖框。
- 保留自动出价保护、金额和权限校验等不改变通信格式的优化，并继续接收其他兼容插件发来的第二代拍卖。

不同客户端的游戏内容和暴雪 API 返回仍可能存在差异，未取得真实数值时 BGNext 会隐藏字段，不会用硬编码数据补齐。

## 0.2.1 - 2026-08-30

这是针对 0.2.0 的启动热修复。

### 修复

- 修复打开插件或设置页时仍访问已停用的旧接收窗口而产生的报错。
- 修复简体中文客户端初始化临时对账按钮时把本地化标记当作文案使用而产生的报错。

除上述修复外，功能、存档和隐私边界与 0.2.0 保持一致。

## 0.2.0 - 2026-08-29

这次更新统一了 BGNext 的插件身份，并加入几项常用操作和安全改进。

### 新增

- 使用全新的 BGNext 图标。
- 小地图按钮现在可以打开金团表格、角色总览和设置。
- 恢复角色总览快捷键。
- 聊天中明确显示的 YY 号码可以手动点击复制，包括普通密语和战网密语。

### 优化

- 插件安装目录由 `BGLite` 更名为 `BGNext`。
- 调整小地图按钮和角色总览入口的点击方式，让常用功能更容易找到。
- 加强拍卖金额校验和消息限流，减少异常消息对正常拍卖的影响。
- 对账数据仅用于当前游戏会话，不再保存其他玩家的历史账单。

### 升级提醒

- 从旧版本升级时，请先删除 `Interface\AddOns\BGLite` 文件夹，再安装新的 `BGNext` 文件夹。
- 不要删除 `WTF` 文件夹或 SavedVariables 中的 `BiaoGe.lua`；原有设置和本地数据会继续使用。
- BGNext 不会自动删除旧插件目录。

### 兼容与隐私

- 继续兼容 BGLite 用户参与当前团队拍卖。
- 保留原有本地存档名称，升级不会主动清空玩家数据。
- YY 复制只处理当前可见消息，不保存聊天历史，也不会自动发送内容。

## 0.1.0 - 2026-08-29

BGNext 的第一个社区版本以 BGLite 2.4.0 为兼容基础，重点补齐个人整理工具，并收紧旧功能的数据边界。

### 新功能

- 重新制作个人心愿清单，支持按副本、难度和首领选择装备，以及本地提醒和手动导入、导出。
- 增加装备过滤方案，方便按自己的职业和需求整理掉落列表。
- 增加自有角色总览，可查看本人角色的团本进度、专业、装备和版本适用资源；周年时光服已完成主要流程验证，其他已登记客户端仍以兼容性矩阵为准。
- 增加当前团购物、交易和邮件核对，只保留最近一个团，最长七天。
- 增加“关于”“更新日志”和“感谢名单”，说明项目来源、版权、隐私边界与公益承诺。

### 修复和优化

- 修复自动出价停止、重复顶价、多个拍卖框重叠，以及自己为最高出价者时提示不清楚的问题。
- 修复心愿清单切换副本或难度后列表不更新、同一物品出现在多个首领或难度时无法分别选择等问题。
- 修复交易价格回填、专精过滤初始状态、账单职业染色、角色总览布局与提示等细节。
- 修复熊猫人客户端勇气点数“本周获得”被放大 100 倍的问题。
- 角色总览采用按需刷新、事件合并和界面对象复用，避免后台持续轮询和重复创建控件。

### 安全与隐私

- 拍卖控制消息只接受当前团长或主拾取者；版本查询增加团队成员校验和内存限流。
- 停止加载旧整表接收、跨团交易历史和邮件历史功能，不读取或迁移这些旧数据。
- 发布包改为从实际运行清单生成，不再把未加载的历史源码一起分发。
- 新增功能数据保存在本地，不含遥测或游戏外自动同步；主动导入、导出会先展示内容。
- 对 BGLite 2.4.0 的运行文件改动逐项登记并由自动检查验证，不把上游代码声明为 BGNext 原创。

## 2.4.0-baseline - 2026-08-26

### 基线

- 导入活动渠道提供的 BGLite 2.4.0 上游分发快照。
- 保留 188 个上游文件，未修改任何运行文件。
- 建立唯一初始提交 `9e0b119c66a644cce0083b5ffe4e59c6c946d0f1`，作为后续完整性检查依据。
