# 贡献指南

感谢你参与 BGNext。项目欢迎代码、测试、错误复现、翻译、文档、视觉设计、安全研究和合规建议。

参与贡献即表示你同意遵守 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)、[SECURITY.md](SECURITY.md)、[隐私政策](docs/policies/PRIVACY.md)和[合规边界](docs/policies/COMPLIANCE.md)。

## 开始之前

除明显的错别字和无行为影响的小型文档修正外，请先创建或认领 GitHub Issue。Issue 应说明：

- 要解决的玩家问题；
- 预期行为和不在范围内的内容；
- 支持的游戏客户端和版本；
- 可能读取、保存或传递的数据；
- 是否涉及自动化、游戏内通信或第三方素材；
- 可验证的完成标准。

涉及数据、通信、自动出价、交易、邮件、外部导出、协议兼容或第三方素材的改动，必须先完成书面评估，不接受直接提交大型实现后再补说明。

## 贡献流程

1. Fork 仓库并从最新 `main` 创建职责单一的分支。
2. 在分支中完成最小必要改动，不混入无关格式化或重构。
3. 运行基线完整性检查及与改动相关的测试。
4. 更新文档、测试记录和 `CHANGELOG.md` 的 `Unreleased` 部分。
5. 提交 Pull Request，完整填写模板中的数据、通信、自动化、素材和兼容性信息。
6. 根据审查意见修改；未经审查的提交不会直接进入稳定版本。

建议的分支名称：

- `feat/<short-name>`：新功能；
- `fix/<short-name>`：错误修复；
- `compat/<client-version>`：游戏版本适配；
- `docs/<short-name>`：文档；
- `security/<short-name>`：不包含敏感细节的安全修复分支名。

建议使用清楚的提交消息，例如：

```text
feat: add local wishlist filtering
fix: prevent duplicate auction messages
docs: clarify exported data fields
```

## 测试要求

所有 Pull Request 至少需要：

```powershell
pwsh -NoProfile -File tools/verify-baseline.ps1
```

修改运行文件时，还应提供：

- 对应游戏客户端和版本；
- 最小复现步骤和修复后的结果；
- 登录、重载界面、进入/离开团队等基本加载测试；
- 与拍卖、交易、账本或通信相关的关键流程测试；
- 与 BGLite 用户混用时的协议和降级结果；
- 错误路径、取消操作和功能关闭后的行为；
- 无新增隐藏采集或游戏外自动上传的验证说明。

截图和日志必须脱敏。

## 本地化要求

BGNext 的语言策略是：简体中文客户端使用简中，繁体中文客户端使用繁中，其他客户端统一回落英文。

- 新增或修改任何玩家可见的静态文字时，同一个改动必须补齐简中、繁中和英文条目；
- `string.format` 占位符的类型、数量和顺序，以及 WoW 颜色与链接标记必须在各语言中保持一致；
- 设计按钮、标签和弹窗时必须检查更长的英文文本，优先使用内容测量或响应式宽度；
- 提交前运行完整 Lua 测试，其中 `tests/test_english_locale.lua` 会检查 BGNext 自有文案覆盖率和占位符安全。

## 基线文件

提交 `5649f58ac2d7d57d3906c97f0aa679bbe5b3da44` 中的 186 个文件构成官方 BGLite 纯净版 2.4.2 基线；2.4.1 与 2.4.0 快照继续作为不可变的历史基线保留。

修改基线文件不是绝对禁止，但必须在 Pull Request 中逐文件说明原因、功能影响、测试证据和平台审核影响。不得通过刷新哈希清单掩盖未说明的运行文件改动。基线清单只在明确导入新的官方 BGLite 基线时更新。

## 数据、安全和通信

运行时贡献必须回答 [合规清单](docs/policies/COMPLIANCE.md)。特别注意：

- 数据默认保存在玩家本地；
- 游戏外导出或传输必须由用户主动触发并提前完整告知；
- 新增游戏内消息必须披露频道、接收方、内容和触发条件；
- 不接受遥测、隐藏通信、后门、远程控制或外部可执行程序；
- 有疑义的实现暂停合并，必要时等待平台书面确认。

## 版权与原创性

只能提交本人原创、已获明确授权或符合原许可证要求的内容。涉及第三方代码、UI、图片、字体、声音或文案时，Pull Request 必须列出来源、作者、许可证或授权依据。

提交贡献即表示你确认拥有必要权利，并允许项目为审查、维护、构建和发布 BGNext 使用该贡献。详情见 [COPYRIGHT.md](COPYRIGHT.md)。

## 审查与合并

维护者依据正确性、范围、测试、兼容性、安全、隐私、合规和权利状态进行审查。以下情况会阻止合并：

- 模板信息不完整或与代码不一致；
- 无法复现、验证或安全回退；
- 引入未披露的数据或通信行为；
- 素材或代码权利不清；
- 试图以下载量、活动排名、赞助或个人关系绕过审查。

合并不等于立即发布。新版本仍需完成发布验证和适用平台审核。

## 贡献者认可

形成可核验实际价值的贡献会按类型记录在 [贡献者名单](docs/community/CONTRIBUTORS.md)，并在相关 Release Notes 中引用 Pull Request 和贡献者。同类贡献不排名；赞助不会换取署名、权限或合并优先级。
